<p id="notice"><%= notice %></p>

<h1>Admins</h1>
<p align="right" class="navbar-text pull-left">
  <% if admin_signed_in? %>
    Logged in as <strong> <%= current_admin.email %></strong>.
    <%= link_to "Logout", destroy_admin_session_path, method: :delete, :class => 'navbar-link'  %>
  <%end %>

</p>
<table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <h3><%= link_to 'Create Book', new_book_path %></h3>
  <h3><%= link_to 'Create Student Account', new_student_path %></h3>
  <h3><%= link_to 'Create Librarian Account', new_librarian_path %></h3>
  <h3><%= link_to 'View all Users', getallusers_path %></h3>

</table>

<br>

<%= link_to 'New Admin', new_admin_path %>
